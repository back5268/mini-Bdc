const { createToolMd } = require('@models');
const { tools } = require('src/data/tools');

const test = async () => {
//   for (const tool of tools) {
//     if (tool) await createToolMd(tool)
//   }
};

test();
